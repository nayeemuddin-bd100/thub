# =============================================================================
# ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and fill in your actual values
# Never commit .env to version control - keep your secrets safe!
# =============================================================================

# -----------------------------------------------------------------------------
# Application Environment
# -----------------------------------------------------------------------------
# Environment mode: 'development' or 'production'
# Controls logging, error handling, and optimization behavior
NODE_ENV=production

# Port number for the application server
# Default: 5000 (Replit's standard port)
PORT=5000

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
# Example: postgresql://user:pass@localhost:5432/travelhub
DATABASE_URL=postgresql://username:password@host:port/database_name

# -----------------------------------------------------------------------------
# Authentication & Session Management
# -----------------------------------------------------------------------------
# Secret key for session encryption (use a strong random string)
# Generate with: openssl rand -base64 32
# IMPORTANT: Change this in production!
SESSION_SECRET=your-super-secret-session-key-change-in-production

# -----------------------------------------------------------------------------
# Payment Processing (Stripe)
# -----------------------------------------------------------------------------
# Stripe secret key (backend) - starts with sk_test_ or sk_live_
# Get from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key

# Stripe publishable key (frontend) - starts with pk_test_ or pk_live_
# Get from: https://dashboard.stripe.com/apikeys
VITE_STRIPE_PUBLIC_KEY=pk_test_your_stripe_publishable_key

# -----------------------------------------------------------------------------
# WhatsApp/SMS Messaging (Twilio)
# -----------------------------------------------------------------------------
# Twilio Account SID
# Get from: https://console.twilio.com/
TWILIO_ACCOUNT_SID=your_twilio_account_sid

# Twilio Auth Token
# Get from: https://console.twilio.com/
TWILIO_AUTH_TOKEN=your_twilio_auth_token

# Twilio WhatsApp-enabled phone number
# Format: whatsapp:+1234567890
TWILIO_WHATSAPP_NUMBER=whatsapp:+1234567890

# =============================================================================
# NOTES FOR DEPLOYMENT ON COOLIFY
# =============================================================================
# 1. Create a new .env file in your Coolify project settings
# 2. Copy these variables and replace placeholder values with real credentials
# 3. Ensure DATABASE_URL points to your production database
# 4. Use production API keys for Stripe (sk_live_ and pk_live_)
# 5. Set NODE_ENV=production
# 6. Generate a strong SESSION_SECRET for production
#
# REQUIRED VARIABLES FOR PRODUCTION:
# - DATABASE_URL (PostgreSQL connection string)
# - SESSION_SECRET (random secure string)
# - STRIPE_SECRET_KEY (if using payments)
# - VITE_STRIPE_PUBLIC_KEY (if using payments)
#
# OPTIONAL VARIABLES:
# - TWILIO_* (only if using WhatsApp/SMS features)
# =============================================================================
