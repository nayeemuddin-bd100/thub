# Production Environment Variables for Docker
# Copy this to .env and update with your production values

# Application
NODE_ENV=production
PORT=5000
APP_PORT=5000

# Database Configuration
# Option 1: Use external production database (like your remote server)
DATABASE_URL=postgresql://inclusivesuites-admin:NYD3CZX316Um@88.99.150.26:5432/inclusivesuites

# Option 2: Use local Docker database (uncomment these if using local postgres)
# DATABASE_URL=postgresql://postgres:postgres123@postgres:5432/travelhub
# DB_USER=postgres
# DB_PASSWORD=postgres123
# DB_NAME=travelhub
# DB_PORT=5433

# Session Secret (REQUIRED - generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
SESSION_SECRET=secretcode123333333322222

# Stripe Keys (REQUIRED)
STRIPE_SECRET_KEY=sk_live_...
VITE_STRIPE_PUBLISHABLE_KEY=pk_live_...
# Alternative key name support
VITE_STRIPE_PUBLIC_KEY=pk_live_...

# WhatsApp Integration (Optional)
WHATSAPP_PHONE_NUMBER=
WHATSAPP_BUSINESS_ID=
WHATSAPP_ACCESS_TOKEN=

# AWS S3 for File Uploads (Optional)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
AWS_S3_BUCKET=

# Video Calling with Daily.co (Optional)
# Get API key from: https://dashboard.daily.co/ (10,000 free minutes/month)
# Leave blank to use demo mode (still works, but with public domain)
DAILY_API_KEY=
